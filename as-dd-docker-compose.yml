version: '3'
services:
  nginx:
    image: 672029928308.dkr.ecr.us-east-1.amazonaws.com/ecr-alphascale-tools:DDnginx
    environment:
      NGINX_METRICS_ENABLED: "${NGINX_METRICS_ENABLED:-false}"
      DD_UWSGI_HOST: "uwsgi"
    volumes:
      - defectdojo_media:/usr/share/nginx/html/media
    ports:
      - "8080:8080"
      - "8443:8443"
  uwsgi:
    image: 672029928308.dkr.ecr.us-east-1.amazonaws.com/ecr-alphascale-tools:DDdjango
    entrypoint: ['/wait-for-it.sh', '${DD_DATABASE_HOST}:${DD_DATABASE_PORT}', '-t', '30', '--', '/entrypoint-uwsgi.sh']
    environment:
      DD_DEBUG: 'False'
      DD_DJANGO_METRICS_ENABLED: "${DD_DJANGO_METRICS_ENABLED:-False}"
      DD_ALLOWED_HOSTS: "${DD_ALLOWED_HOSTS:-*}"
      DD_DATABASE_URL: ${DD_DATABASE_URL}
      DD_CELERY_BROKER_URL: ${DD_CELERY_BROKER_URL}
      DD_SECRET_KEY: "${DD_SECRET_KEY:-hhZCp@D28z!n@NED*yB!ROMt+WzsY*iq}"
      DD_CREDENTIAL_AES_256_KEY: "${DD_CREDENTIAL_AES_256_KEY:-&91a*agLqesc*0DJ+2*bAbsUZfR*4nLw}"
      DD_LOGIN_REDIRECT_URL: '/tools/defect-dojo'
      DD_LOGIN_URL: '/tools/defect-dojo/login'
      DD_STATIC_URL: '/tools/defect-dojo/static/'
    volumes:
        - "defectdojo_media:${DD_MEDIA_ROOT:-/app/media}"
  celerybeat:
    image: 672029928308.dkr.ecr.us-east-1.amazonaws.com/ecr-alphascale-tools:DDdjango
    entrypoint: ['/wait-for-it.sh', '${DD_DATABASE_HOST}:${DD_DATABASE_PORT}', '-t', '30', '--', '/entrypoint-celery-beat.sh']
    environment:
      DD_DATABASE_URL: ${DD_DATABASE_URL}
      DD_CELERY_BROKER_URL: ${DD_CELERY_BROKER_URL}
      DD_SECRET_KEY: "${DD_SECRET_KEY:-hhZCp@D28z!n@NED*yB!ROMt+WzsY*iq}"
      DD_CREDENTIAL_AES_256_KEY: "${DD_CREDENTIAL_AES_256_KEY:-&91a*agLqesc*0DJ+2*bAbsUZfR*4nLw}"
      DD_LOGIN_REDIRECT_URL: '/tools/defect-dojo'
      DD_LOGIN_URL: '/tools/defect-dojo/login'
      DD_STATIC_URL: '/tools/defect-dojo/static/'
  celeryworker:
    image: 672029928308.dkr.ecr.us-east-1.amazonaws.com/ecr-alphascale-tools:DDdjango
    entrypoint: ['/wait-for-it.sh', '${DD_DATABASE_HOST}:${DD_DATABASE_PORT}', '-t', '30', '--', '/entrypoint-celery-worker.sh']
    environment:
      DD_DATABASE_URL: ${DD_DATABASE_URL}
      DD_CELERY_BROKER_URL: ${DD_CELERY_BROKER_URL}
      DD_SECRET_KEY: "${DD_SECRET_KEY:-hhZCp@D28z!n@NED*yB!ROMt+WzsY*iq}"
      DD_CREDENTIAL_AES_256_KEY: "${DD_CREDENTIAL_AES_256_KEY:-&91a*agLqesc*0DJ+2*bAbsUZfR*4nLw}"
      DD_LOGIN_REDIRECT_URL: '/tools/defect-dojo'
      DD_LOGIN_URL: '/tools/defect-dojo/login'
      DD_STATIC_URL: '/tools/defect-dojo/static/'
    volumes:
        - "defectdojo_media:${DD_MEDIA_ROOT:-/app/media}"
  rabbitmq:
    image: 672029928308.dkr.ecr.us-east-1.amazonaws.com/ecr-alphascale-tools:DDrabbitmq
volumes:
  defectdojo_media: {}
